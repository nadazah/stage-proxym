const Item = (): JSX.Element => {
	// const user = useFragment(UserFragment, props.user);
	// return (
	// 	<div>
	// 		<h3>{user.name}</h3>
	// 		<p>{user.email}</p>
	// 	</div>
	// );
	return <></>
};

export default Item;
